[project]
name = "pgiscluster"
description = "Python classes intended to orchestrate Extract, Transform, & Load (ETL) Data Pipelines and Machine Learning (ML) tasks."
readme = "README.md"
requires-python = ">=3.14.7"
authors = [{ name = "DragonBishop", email = "276789280+DragonBishop@users.noreply.github.com" }]
license = "MIT"
classifiers = [
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.14",
]
dependencies = [
  "geopandas>=1.1.4",
  "hvac>=2.4.0",
  "kubernetes>=36.0.3",
  "pandas>=3.0.5",
  "psycopg[binary]>=3.3.4",
  "sqlalchemy>=2.0.52",
]
dynamic = ["version"]

[project.optional-dependencies]
common = [
  "ipykernel>=7.3.0",
  "prefect>=3.8.5",
]
etl = [
  "dbt-core>=1.12.3",
  "dbt-postgres>=1.11.0",
  "dlt>=1.30.0",
  "pgiscluster[common]",
]
ml = [
  "scikit-learn>=1.3.2",
  "bentoml>=1.2.0",
  "mlflow>=3.16.0",
  "pgiscluster[common]",
]

[dependency-groups]
dev = [
  "pytest>=9.1.1",
  "pytest-cov>=7.1.0",
  "prek>=0.5.0",
]
lint = [
  "ruff>=0.16.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.coverage.report]
exclude_also = [
  "def __repr__",
  "if self.debug:",
  "if settings.DEBUG",
  "raise AssertionError",
  "raise NotImplementedError",
  "if 0:",
  "if __name__ == .__main__.:",
  "if TYPE_CHECKING:",
  'class .*\bProtocol\):',
  '@(abc\.)?abstractmethod',
]

[tool.hatch.version]
path = "src/pgiscluster/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/pgiscluster"]

[tool.hatch.build.targets.sdist]
exclude = [
  ".venv",
  "uv.lock",
]


[tool.rumdl]
disable = ["MD013"]
exclude = ["CHANGELOG.md"]

[tool.rumdl.MD024]
siblings-only = true

[tool.uv]
default-groups = ["dev"]
